## 1.0.0

* Added Halo device support
* Updated `frame_ble` to `4.0.0` — `BrilliantDeviceType` enum, Halo audio characteristic, Android BLE improvements
* Updated `frame_msg` to `3.0.0` — `RxClick`, `TxTextPage`/`CircularTextLayout`, updated Lua libraries with queue-based data handling, float IMU values, and sprite header changes

## 0.1.0

* Initial release of the Brilliant Labs SDK meta-package containing the `frame_ble` and `frame_msg` packages.