name: simple_frame_app
description: "Flutter and Lua quickstart app scaffolding and standard library functions for Brilliant Frame development on Android/iOS"
version: 8.0.0
publish_to: 'none'
homepage:
repository: https://github.com/CitizenOneX/simple_frame_app

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  image: ^4.2.0
  logging: ^1.2.0
  frame_msg:
    path: ../frame_msg
  frame_ble:
    path: ../frame_ble

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
