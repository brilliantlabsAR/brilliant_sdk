local a=require("view.min")local b=setmetatable({},{__index=a})b.__index=b;function b:new(c,d,e,f)local g=a.new(self,c or 0,d or 0,e or 256,f or 256)g.children={}return g end;function b:add_child(h,i)self[h]=i;table.insert(self.children,h)end;function b:update(j)for k,h in ipairs(self.children)do local l=self[h]if l and l.update then l:update(j)if l.is_dirty then self.is_dirty=true end end end end;function b:render()if not self.is_dirty then return end;for k,h in ipairs(self.children)do local l=self[h]if l and l.is_dirty or frame.HARDWARE_VERSION=='Frame'then l:clear()l:render()l.is_dirty=false end end;self.is_dirty=false end;return b