local a=require("view.min")local b=setmetatable({},{__index=a})b.__index=b;function b:set_recording(c)self.is_recording=c;self:invalidate()end;function b:render()if frame.HARDWARE_VERSION~='Frame'then local d=self.is_recording and 0xF00000 or 0x000000;frame.display.circle(self.x+8,self.y+10,8,d,true)else if self.is_recording then frame.display.assign_color('RED',255,0,0)frame.display.bitmap(self.x+8,self.y+10,16,2,3,string.rep("\xFF",32))end end;self.is_dirty=false end;return b